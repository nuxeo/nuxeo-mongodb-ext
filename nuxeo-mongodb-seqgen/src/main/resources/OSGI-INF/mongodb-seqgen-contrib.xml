<?xml version="1.0"?>
<component name="org.nuxeo.mongodb.seqgen.contrib">

  <documentation>
    MongoDB Sequence Generator uses MongoDBComponent. By default, if no MongoDB connection with id 'sequence'
    exists, we will use the 'default' one.

    You could override the collection name used to store sequence by contributing to ConfigurationService like this:
    <code>
      <extension target="org.nuxeo.runtime.ConfigurationService" point="configuration">
        <property name="nuxeo.mongodb.seqgen.collection.name">myCollection</property>
      </extension>
    </code>
    By default, backend will use 'sequence' as collection name.
  </documentation>

  <require>org.nuxeo.mongodb.core.MongoDBComponent</require>

</component>
